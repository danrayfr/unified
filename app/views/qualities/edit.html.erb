<%= provide(:title, "Quality assessment") %>

<div class="container my-12 mx-auto px-4 md:px-12">
  <div class="flex flex-col items-start justify-between pb-6 space-y-4 lg:items-center lg:space-y-0 lg:flex-row">
    <ol class="flex flex-wrap pt-1 mr-12 bg-transparent rounded-lg sm:mr-16">
      <li class="leading-normal text-sm">
        <a class="opacity-50 text-slate-700"><%= @account.slug %></a>
      </li>
      <li class="text-sm pl-2 capitalize leading-normal text-slate-700 before:float-left before:pr-2 before:text-gray-600 before:content-['/']" aria-current="page"><%= link_to "Quality Assurance", account_qualities_url %></li>
      <li class="text-sm pl-2 capitalize leading-normal text-slate-700 before:float-left before:pr-2 before:text-gray-600 before:content-['/']" aria-current="page"><%= link_to @quality.uid, :back %></li>
      <li class="text-sm pl-2 capitalize leading-normal text-slate-700 before:float-left before:pr-2 before:text-gray-600 before:content-['/']" aria-current="page">Edit</li>
    </ol>

    <div class="space-y-6 md:space-x-2 md:space-y-0">
      <%= link_to account_coaching_url(@account, @quality), data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'}, method: :delete, class:"inline-flex items-center justify-center px-4 py-1 space-x-1 bg-coral text-white rounded-md shadow animate-bounce hover:bg-graphite" do %>
        <span>Delete QA</span>
      <% end if current_user.manager? %>
    </div>
  </div>

  <div class="space-y-10 divide-y divide-gray-900/10">
    <div class="grid grid-cols-1 gap-x-8 pt-10 md:grid-cols-3">
      <div class="px-4 sm:px-0">
        <h2 class="text-base font-semibold leading-7 text-gray-900">Create a new account</h2>
        <p class="mt-1 text-sm leading-6 text-gray-600">
          Make some selections, fill in some boxes, and go live in minutes.
        </p>
         <p class="mt-4">
            <%= render 'shared/error_messages', object: @quality %>
         </p>
      </div>  
      <%= render "form", account: @account, quality: @quality %>
    </div>
    </div>
  </div>
</div>
